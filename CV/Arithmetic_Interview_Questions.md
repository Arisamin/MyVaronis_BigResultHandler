# Arithmetic Interview Questions Without Operators or Loops

This document collects common interview questions that require implementing arithmetic operations without using standard operators or loops. Each question is listed with space for your answer and notes.

---

## Questions

1. Implement addition of two integers without using the `+` operator (use bitwise operations).
2. Implement subtraction of two integers without using the `-` operator.
3. Implement multiplication of two numbers without using the `*` operator or loops (use recursion).
4. Implement division of two numbers without using the `/` operator or loops.
5. Implement exponentiation (power) without using the `*` operator or loops.
6. Multiply two numbers using bitwise operations only.
7. Add two numbers using recursion only.
8. Subtract two numbers using recursion only.
9. Divide two numbers using recursion only.
10. Implement arithmetic operations using only bitwise operators.

---

## Answers & Notes

<!-- Add your answers below each question as you process them. -->
